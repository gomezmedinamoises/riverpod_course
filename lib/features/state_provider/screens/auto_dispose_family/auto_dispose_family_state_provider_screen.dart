import 'package:flutter/material.dart';

class AutoDisposeStateProviderFamilyScreen extends StatelessWidget {
  const AutoDisposeStateProviderFamilyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Auto Dispose Family State Provider'),
      ),
      body: const Center(
        child: Text('Auto Dispose Family State Provider'),
      ),
    );
  }
}
