import 'package:flutter/material.dart';

class FamilyStateProviderScreen extends StatelessWidget {
  const FamilyStateProviderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Family State Provider'),
      ),
      body: const Center(
        child: Text('Family State Provider'),
      ),
    );
  }
}
